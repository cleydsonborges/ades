# HIPAA Rule Pack (Template)

Place machine-readable policy definitions here (YAML/JSON). The Compliance Guard agent loads packs from this directory at startup.

Example rule categories:

- PHI column detection and masking requirements
- Minimum necessary field selection
- Audit column requirements on sinks
